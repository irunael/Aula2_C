#include <stdio.h>
#include <stdlib.h>
#include <time.h>

void sorteio(){
    //nao entendi o que faz
    srand(time(NULL));
    //gera um numero aleatorio entre 0 e 99.
    int num_aleatorio = (rand() % 100) + 2;
}

int main(){
    int num;

    printf("Um número aleatório foi sorteado, estando entre 1 e 100.\nTente adivinhar qual é o número.");
    
}